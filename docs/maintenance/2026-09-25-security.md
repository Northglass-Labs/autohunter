# Security maintenance — 2026-09-25

Next.js/ESLint 16.3.6, Sharp 0.35.4, Vitest 4.1.11 and collector adm-zip 0.6.1 remove the application and collector dependency findings.

## Verification

Catcht unit tests, lint and production build plus collector tests passed locally. The existing PR CI, including disposable database and browser checks, passed before this documentation-only commit. Both npm audits report zero findings.

These are source and dependency checks. A merged commit alone does not establish that an installed or hosted runtime has been updated. Deployment and local uptake receipts belong in the estate maintenance report.

## Reproduction and rollback

Install from the committed lockfile using the repository's documented runtime, then rerun its tests/build and audit every nested dependency root. Preserve local work and back up persistent data before restarting a deployed app. Roll back with the previous verified release artifact or a focused revert of this maintenance change; do not reset a dirty checkout.

Maintenance PR: https://github.com/Northglass-Labs/autohunter/pull/18
